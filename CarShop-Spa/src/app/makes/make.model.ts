export class Make{
    constructor (
        public id: Number,
        public name: string,
        public country: string,
        public createdAt: string,
        public updatedAt: string,
        public url: string
    ) {}
}